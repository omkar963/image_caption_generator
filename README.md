# image_caption_generator
A deep learning project that generates natural language descriptions from images. The system combines InceptionV3 for image feature extraction and an LSTM-based decoder for caption generation.  Key Features:  Uses MS COCO dataset for image–caption pairs  CNN (InceptionV3) for visual feature extraction . Generate captions for unseen images 
